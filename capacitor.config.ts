import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'biz.pambo.marketplace',
  appName: 'Pambo Marketplace',
  webDir: 'dist'
};

export default config;
